# ## Dependencies ##
# The node_modules folder contains all the installed packages.
# It can be very large and should always be re-created by running `npm install`.
/node_modules

# ## Environment Variables ##
# This file contains sensitive data like database passwords, API keys, and secrets.
# NEVER commit this file. A .env.example file can be committed instead as a template.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ## Logs ##
# Log files generated by npm or other tools.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ## Prisma ##
# The Prisma client is generated into node_modules, but in case of schema changes
# or specific generator outputs, it's good practice to be explicit.
# Local SQLite database files for development.
*.db
*.db-journal

# ## Operating System Files ##
# Files generated by macOS, Windows, and Linux systems.
.DS_Store
Thumbs.db
desktop.ini

# ## IDE & Editor Directories ##
# Configuration files for popular code editors.
.vscode/
.idea/
*.swp
*.swo

# ## Build & Production ##
# Output directories for compiled code (e.g., from TypeScript).
/dist
/build